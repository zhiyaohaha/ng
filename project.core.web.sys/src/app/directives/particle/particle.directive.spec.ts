import { ParticleDirective } from './particle.directive';

describe('ParticleDirective', () => {
  it('should create an instance', () => {
    const directive = new ParticleDirective();
    expect(directive).toBeTruthy();
  });
});
